name = "income-bot"
main = "src/index.js"
compatibility_date = "2024-09-23"

# BOT_TOKEN is set as a secret after deployment, in the Cloudflare dashboard
# (Worker → Settings → Variables and Secrets), never committed here.
